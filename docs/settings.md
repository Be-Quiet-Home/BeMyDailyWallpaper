Settings policy:
  ? settings live in AppSettings
  ? no settings logic in MainWindow
  ? no settings logic in providers
  ? no settings logic in WallpaperSetter
  ? persistence later under Haiku settings path
  
